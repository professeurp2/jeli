fastapi>=0.115
# Forms and file uploads on the dashboard
python-multipart>=0.0.9
uvicorn[standard]>=0.30
httpx>=0.27
python-telegram-bot>=22.0,<23
pydantic-settings>=2.4
psycopg[binary,pool]>=3.2
google-genai>=1.0
# Text-to-speech: free, no API key, natural voices in French and English
edge-tts>=7.0
# Timezone database for exports (Windows has none built in)
tzdata
# System certificate store on Windows/macOS (HTTPS-inspecting antivirus, corporate proxies)
truststore
# Documents: reading PDF and Word files, writing translated PDFs
pypdf>=5
python-docx>=1.1
reportlab>=4.2

# The memory's spare sense: embeddings computed locally, so a Google outage does not blind the
# search. Small multilingual model (~220 MB), no key and no quota — see app/kb/local_embeddings.py
fastembed>=0.4
